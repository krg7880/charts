{{- if and (.Values.dashboard.auth.basicAuth.enabled) (.Values.dashboard.auth.basicAuth.secret)  }}
apiVersion: v1
kind: Secret
metadata:
  name: traefik-dashboard-admin
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  users: |2
   {{ .Values.dashboard.auth.basicAuth.secret.users }}
{{- end -}}
